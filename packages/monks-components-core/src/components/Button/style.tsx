import { StyleSheet } from '../../hooks/useStyles';

export const buttonStyleSheet: StyleSheet = ({
  color,
  font,
  pattern,
  ui,
  unit,
  transition,
}) => ({
  button: {
    color: '#f00',
  },
});
